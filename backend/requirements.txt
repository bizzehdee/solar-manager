# Runtime dependencies (plan.md §3). Kept lean so the native Pi install stays trivial.
fastapi>=0.115
uvicorn[standard]>=0.30
pyyaml>=6.0
pymodbus>=3.6          # async Modbus RTU/TCP client (Phase 1 real-hardware transport)
pyserial>=3.5          # serial backend for pymodbus's RTU client (RS485 over /dev/ttyUSB*)
httpx>=0.27            # async HTTP client for the Open-Meteo forecast fetch (Phase 4)
python-multipart>=0.0.9  # multipart form parsing for the backup-restore upload (Phase 8 / T091)
paho-mqtt>=2.0         # MQTT publish to the user's broker + HA discovery (Later / L07); pure-Python, no broker bundled
