"""Single source of truth for the running version.

In development this is the placeholder below. The release workflow (plan.md §13)
stamps the `x.y` parsed from a `version/x.y` git tag into this file at build time —
do not hand-edit the version to cut a release.
"""

__version__ = "0.0.0-dev"
