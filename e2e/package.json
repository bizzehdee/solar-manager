{
  "name": "solarvolt-e2e",
  "private": true,
  "description": "Playwright integration tests — drive the full app on the dummy (plan.md §21)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
